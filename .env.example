# ============================================
# ShortLinker Frontend 环境变量配置示例
# ============================================
#
# 配置优先级（开发模式）:
# 1. 环境变量 (VITE_*)
# 2. 运行时注入配置 (window.__APP_CONFIG__)
# 3. 默认值
#
# 配置优先级（生产模式）:
# 1. 运行时注入配置 (window.__APP_CONFIG__)
# 2. 默认值
#
# 注意: 所有环境变量必须以 VITE_ 开头才能在前端代码中访问
# ============================================

# 基础路径（用于路由）
# 默认值: /
VITE_BASE_URL=/

# 管理后台路由前缀
# 默认值: /admin
# 开发模式可以覆盖此值
# VITE_ADMIN_ROUTE_PREFIX=/admin

# 健康检查路由前缀
# 默认值: /health
# 开发模式可以覆盖此值
# VITE_HEALTH_ROUTE_PREFIX=/health

# API 基础 URL
# 默认值: 空字符串（使用当前域名）
# 开发模式示例: http://localhost:8080
# 生产模式: 由后端注入或留空
# VITE_API_BASE_URL=http://localhost:8080

# 版本号
# 默认值: unknown
# 开发模式可以设置为 dev-local 等
# 生产模式: 由构建脚本注入
# VITE_VERSION=dev-local
